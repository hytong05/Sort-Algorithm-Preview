#include <bits/stdc++.h>
using namespace std;

main()
{
    freopen("TEST10.txt","r",stdin);
    int N=1e6;
    float a[N];
    for (int i=0;i<N;i++)
        cin >> a[i];
    sort (a, a+N);
}
